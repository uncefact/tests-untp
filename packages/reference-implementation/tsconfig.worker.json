{
  "_comment": "Copied to /app/tsconfig.json in the runtime image and named by TSX_TSCONFIG_PATH there, so tsx resolves the @/ alias against the flattened layout. The checkout uses tsconfig.json; this file exists only because that one maps @/ from the package root, which the image does not have.",
  "compilerOptions": {
    "target": "ES2022",
    "module": "esnext",
    "moduleResolution": "bundler",
    "esModuleInterop": true,
    "resolveJsonModule": true,
    "baseUrl": ".",
    "paths": {
      "@/*": ["./src/*"],
      "@prisma/client": ["./src/lib/prisma/generated"]
    }
  }
}
