{
  "warnings": [
    {{#each warnings}}
      {
        "fieldName": "{{params.additionalProperty}}",
        "message": "Additional property found: '{{params.additionalProperty}}'."
      }
      {{#unless @last}},{{/unless}}
    {{/each}}
  ]
}