{
  "testingCredential": "{{type}} {{version}}",
  "path": "{{dataPath}}",
  "result": "{{result}}",
  "errors": [
    {{#each errors}}
      {
        "fieldName": "{{instancePath}}",
        "errorType": "{{keyword}}",
        {{#if params.allowedValues}}
        "allowedValues": {{{jsonArray params.allowedValues}}},
        {{/if}}
        "message": "{{instancePath}} field {{message}}"
      }
      {{#unless @last}},{{/unless}}
    {{/each}}
  ]
}