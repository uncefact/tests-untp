{
  "validationWarnings": [
    {{#each validationWarnings}}
      {
        "fieldName": "{{params.additionalProperty}}",
        "message": "This schema {{message}}"
      }
      {{#unless @last}},{{/unless}}
    {{/each}}
  ]
}